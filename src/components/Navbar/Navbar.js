import './Navbar.css';

const Navbar = () => {
	return (
		<nav className="Navbar">
			<h1>Where in the world?</h1>

			<div className="theme">
				<button>Dark Mode</button>
			</div>
		</nav>
	);
};

export default Navbar;
